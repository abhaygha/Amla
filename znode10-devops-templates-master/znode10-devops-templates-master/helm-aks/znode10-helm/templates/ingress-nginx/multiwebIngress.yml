{{- if (and .Values.multiwebIngress (or .Values.multiwebIngress.enabled true)) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: multistore-ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: {{ .Values.global.ingressClassName | default "nginx" }}

  tls:
    - hosts:
        {{- range .Values.multiwebIngress.tls.services | default list }}
        - {{ . }}{{ $.Values.global.domain }}
        {{- end }}
      secretName: {{ .Values.multiwebIngress.tls.secretName | default "default-tls-secret" }}
  rules:
    {{- range .Values.multiwebIngress.rules | default list }}
    {{- $rule := . }}
    {{- $svcName := .serviceName }}
    {{- $svcPort := .servicePort }}
    - host: {{ $rule.name }}{{ $.Values.global.domain }}
      http:
        paths:
          {{- range $rule.paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $svcName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
