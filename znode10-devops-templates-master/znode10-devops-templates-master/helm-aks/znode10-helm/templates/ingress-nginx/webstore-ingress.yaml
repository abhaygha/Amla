{{- if (and .Values.webIngress (or .Values.webIngress.enabled true)) }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webstore-ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
spec:
  ingressClassName: {{ .Values.global.ingressClassName | default "nginx" }}

  tls:
    - hosts:
        {{- range .Values.webIngress.tls.services }}
        - {{ . }}{{ $.Values.global.domain }}
        {{- end }}
      secretName: {{ .Values.webIngress.tls.secretName }}
  rules:
    {{- range .Values.webIngress.rules }}
    {{- $rule := . }}
    {{- $svcName := .serviceName }}
    {{- $svcPort := .servicePort }}
    - host: {{ $rule.name }}{{ $.Values.global.domain }}
      http:
        paths:
          {{- range $rule.paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $svcName }}
                port:
                  number: {{ $svcPort }}
          {{- end }}
    {{- end }}
{{- end }}
